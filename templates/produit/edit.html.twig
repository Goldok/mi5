{% extends 'base.html.twig' %}

{% block title %}BackOffice-Produit-edit{% endblock %}

{% block body %}
    <h1>Editer {{produit.libelle}}</h1>

    {{ include('produit/_form.html.twig', {'button_label': 'Enregistrer'}) }}

    {{ include('produit/_delete_form.html.twig') }}

    <a href="{{ path('produit_index') }}">retour</a>

{% endblock %}
