{% extends 'base.html.twig' %}

{% block title %}BackOffice-Produit-show{% endblock %}

{% block body %}
    <h1>{{ produit.libelle }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ produit.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ produit.libelle }}</td>
            </tr>
            <tr>
                <th>Texte</th>
                <td>{{ produit.texte }}</td>
            </tr>
            <tr>
                <th>Visuel</th>
                <td>                     <div class="card" style="width: 10rem;">
                        <img class="card-img-top" src="{{ asset(produit.visuel) }}" alt="{{produit.libelle}}">
                    </div>
                </td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ produit.prix }} €</td>
            </tr>
        </tbody>
    </table>

    <form class="form-inline my-2 my-lg-0" action="{{ path('produit_edit', {'id': produit.id}) }}" method="GET">
        <button class="btn btn-primary" type="submit">
            Editer
        </button>
    </form>

    {{ include('produit/_delete_form.html.twig') }}


    <a href="{{ path('produit_index') }}">retour</a>


{% endblock %}
