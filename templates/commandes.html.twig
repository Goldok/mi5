{% extends "base.html.twig" %}
{% block title %}Mes Commandes {% endblock %}

{% block body %}


    <div class="row">

        <table class="table table-striped table-sm table-hover">
            <thead>
            <tr>
                <th></th>
                <th>N°Commande</th>
                <th>Date</th>
                <th>Produit</th>
                <th>Montant</th>
                <th>Statut</th>
            </tr>
            </thead>
            {% for commande in commandes %}
                <tbody>
                <tr>
                    <td></td>
                    <td>
                        {{ commande['commande'].getId() }}
                    </td>
                    <td>
                      {{ commande['commande'].getDateCommande() }}

                    </td>
                    <td class="">
                        {{ commande['quantite'] }}
                    </td>
                    <td>
                        {{ commande['prix'] | currency_convert | format_currency(app.session.get('current_currency'), {rounding_mode: 'floor'})}}

                    </td>
                    <td class="">
                  {{ commande['commande'].statut }}

                    </td>

                </tr>
                </tbody>




            {% endfor %}

        </table>
    </div>
{% endblock %}

